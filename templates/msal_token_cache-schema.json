{
    "AccessToken": {
        "-login.microsoftonline.com-accesstoken-{{SPN_CLIENT_ID}}-{{TENANT_ID}}-https://management.core.windows.net//.default": {
            "credential_type": "AccessToken",
            "secret": "{{TOKEN}}",
            "home_account_id": null,
            "environment": "login.microsoftonline.com",
            "client_id": "{{SPN_CLIENT_ID}}",
            "target": "https://management.core.windows.net//.default",
            "realm": "{{TENANT_ID}}",
            "token_type": "Bearer",
            "cached_at": "{{CACHED_AT}}",
            "expires_on": "{{EXPIRES_ON}}",
            "extended_expires_on": "{{EXTENDED_EXPIRES_ON}}"
        }
    },
    "AppMetadata": {
        "appmetadata-login.microsoftonline.com-{{SPN_CLIENT_ID}}": {
            "client_id": "{{SPN_CLIENT_ID}}",
            "environment": "login.microsoftonline.com"
        }
    }
}